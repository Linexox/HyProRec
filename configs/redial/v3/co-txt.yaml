# START: Qwen v3 co-occurrence plus text-view experiment.
model:
  backbone_name_or_path: /data2/lhf/.MODEL/Qwen2.5-Omni-3B
  freeze_backbone: true
  beta: 0.75
  num_soft_prompt_tokens: 10
  train_special_tokens: false
  hypergraph_hidden_dim: 1024
  hypergraph_output_dim: 256
  hypergraph_num_layers: 3
  hypergraph_dropout: 0.0
  use_hypergraph_encoder: true
  use_source_projector: false
  grounding_weight: 1.0
  grounding_ga_sa_weight: 1.0
  grounding_ga_sn_weight: 1.0
  grounding_sa_sn_weight: 0.0
  grounding_temperature: 0.07
  item_table_init: aligned_content
  train_item_table: true
  recommendation_hidden_dim: 768
  recommendation_dropout: 0.0
  recommendation_temperature: 0.07

data:
  dataset_path: data/lhf-redial
  hyperedge_table_path: data/lhf-redial/hyperedge_table_v3.json
  embeddings_dir_name: embeddings_v3
  content_table_path: data/lhf-redial/content_tables_v3/txt.pt
  views: [co, txt]
  topk: 3
  khop: 2
  max_length: 1024
  max_history_tokens: 150
  max_response_tokens: 64

training:
  output_dir: outputs/redial/v3/co-txt
  do_train: true
  do_eval: true
  do_predict: false
  per_device_train_batch_size: 1
  per_device_eval_batch_size: 1
  gradient_accumulation_steps: 8
  num_train_epochs: 8
  learning_rate: 0.00005
  weight_decay: 0.0
  warmup_steps: 0.1
  bf16: true
  gradient_checkpointing: true
  gradient_checkpointing_kwargs:
    use_reentrant: false
  eval_strategy: epoch
  save_strategy: epoch
  logging_strategy: steps
  logging_steps: 20
  load_best_model_at_end: true
  metric_for_best_model: eval_loss
  greater_is_better: false
  save_total_limit: 2
  report_to: wandb
  run_name: co-txt
  seed: 42
  data_seed: 42
  dataloader_num_workers: 4
  dp_replicate_size: 1
  dp_shard_size: 1
# END: Qwen v3 co-occurrence plus text-view experiment.
